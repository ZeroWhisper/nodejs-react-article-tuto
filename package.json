{
  "name": "nodejs-react-article-tuto",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev_client_start": "cd client && npm install && npm run start",
    "dev_server_start": "cd server && npm install && npm run start",
    "dev_start": "concurrently --kill-others-on-fail \"npm run dev_server_start\" \"npm run dev_client_start\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^4.0.1",
    "nodemon": "^1.18.11"
  }
}
